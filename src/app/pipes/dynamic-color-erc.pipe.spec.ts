import { DynamicColorErcPipe } from './dynamic-color-erc.pipe';

describe('DynamicColorErcPipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicColorErcPipe();
    expect(pipe).toBeTruthy();
  });
});
