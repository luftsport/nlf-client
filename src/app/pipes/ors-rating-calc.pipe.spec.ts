import { OrsRatingCalcPipe } from './ors-rating-calc.pipe';

describe('OrsRatingCalcPipe', () => {
  it('create an instance', () => {
    const pipe = new OrsRatingCalcPipe();
    expect(pipe).toBeTruthy();
  });
});
