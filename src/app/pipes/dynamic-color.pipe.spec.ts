import { DynamicColorPipe } from './dynamic-color.pipe';

describe('DynamicColorPipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicColorPipe();
    expect(pipe).toBeTruthy();
  });
});
