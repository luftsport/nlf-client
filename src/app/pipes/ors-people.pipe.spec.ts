import { NlfOrsPeoplePipe } from './ors-people.pipe';

describe('NlfOrsPeoplePipe', () => {
  it('create an instance', () => {
    const pipe = new NlfOrsPeoplePipe();
    expect(pipe).toBeTruthy();
  });
});
