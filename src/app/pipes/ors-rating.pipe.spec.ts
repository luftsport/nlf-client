import { OrsRatingPipe } from './ors-rating.pipe';

describe('OrsRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new OrsRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
