import { HighliteTextPipe } from './highlite-text.pipe';

describe('HighliteTextPipe', () => {
  it('create an instance', () => {
    const pipe = new HighliteTextPipe();
    expect(pipe).toBeTruthy();
  });
});
